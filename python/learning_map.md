🧠 Carte des Notions Avancées — Python Exploration
🔁 Itération & Génération

yield, yield from

Générateurs personnalisés

iter(), next(), protocoles itérables/itérateurs

    Générateurs avec send(), close()

💡 Programmation Fonctionnelle

Fonctions comme objets

*args, **kwargs avancés

Décorateurs simples + paramétrables

functools (lru_cache, partial, etc.)

    Closures, fonctions imbriquées

🧰 Gestion des ressources

Context managers (with, __enter__, __exit__)

contextlib (contextmanager, suppress, etc.)

    Création de context managers personnalisés

⚙️ Concurrence & Parallélisme

threading (threads simples)

concurrent.futures.ThreadPoolExecutor

multiprocessing

    Communication entre processus (Queue, Pipe, etc.)

🌀 Programmation Asynchrone

async def, await, async for, async with

asyncio.run(), create_task()

Boucle d’événements, coroutines

    Différences entre async vs thread vs process

📦 Structuration & Packaging

__init__.py, modules/packages

pyproject.toml, setup simple

Installation avec pip install .

    Entry points (console_scripts)

🧪 Tests avancés

pytest avec fixtures complexes

monkeypatch, tmp_path, caplog

Tests paramétrés

    Mocks avec unittest.mock

✨ Bonus Pythonic

Dunder methods (__str__, __repr__, __eq__, etc.)

dataclasses, attrs, pydantic (introduction)

Métaclasses (juste pour voir !)

Typage avancé (TypeVar, Protocol, Literal)